# Исправление проблемы OTA обновления

## Описание проблемы

При загрузке прошивки через OTA происходила ошибка:
```
[OTA] OTA End: 1076752 bytes total
[311412][E][Updater.cpp:629] end(): premature end: res:0, pos:1073152/6553600
[OTA] ERROR: Update.end() failed - Aborted
[OTA] Error code: 12
```

**Причина**: Загрузка прерывалась на ~1 МБ из 6.25 МБ ожидаемого размера раздела OTA.

## Выполненные исправления

### 1. Добавлено подробное логирование в [`src/apiota.cpp`](src/apiota.cpp)

Добавлены следующие переменные для отслеживания состояния загрузки:
- `otaStartTime` - время начала загрузки
- `otaTotalBytesWritten` - общее количество записанных байт
- `otaChunkCount` - количество обработанных чанков

Добавлено логирование:
- Размер файла и свободной памяти до OTA
- Информация о разделах (running и update)
- Логирование каждые 50 чанков загрузки
- Скорость загрузки и время выполнения
- Подробная диагностика при ошибках

### 2. Исправлено использование [`Update.begin()`](src/apiota.cpp:108)

**Проблема**: Ранее использовался размер раздела OTA (6.25 МБ), что вызывало проблемы с памятью.

**Решение**: Теперь используется размер загружаемого файла:
```cpp
size_t updateSize = (upload.totalSize > 0) ? upload.totalSize : update->size;
Update.begin(updateSize);
```

Это позволяет избежать проблем с большими разделами и ограниченной памятью.

### 3. Добавлена проверка размера файла

Добавлена константа `OTA_MAX_FILE_SIZE` (6.25 МБ) и проверка размера файла перед началом OTA:
```cpp
if (upload.totalSize > OTA_MAX_FILE_SIZE) {
    apiota_log("ERROR: File too large: " + String(upload.totalSize) + " bytes");
    otaErrorMessage = "File too large";
    return;
}
```

### 4. Улучшена обработка ошибок

Добавлена обработка состояния `UPLOAD_FILE_ABORTED` для отслеживания прерванных загрузок:
```cpp
else if (upload.status == UPLOAD_FILE_ABORTED) {
    apiota_log("ERROR: Upload aborted");
    apiota_log("Total written before abort: " + String(otaTotalBytesWritten) + " bytes");
    otaErrorMessage = "Upload aborted";
}
```

## Новое логирование

Пример новых логов при успешной загрузке:
```
[OTA] POST /api/ota/upload
[OTA] OTA Start: firmware.bin
[OTA] Content length: 1058311 bytes
[OTA] Free heap before OTA: 253188 bytes
[OTA] Free PSRAM: 8388608 bytes
[OTA] Running partition: app0 @ 0x10000, size: 6553600 bytes
[OTA] Update partition: app1 @ 0x650000, size: 6553600 bytes
[OTA] Update partition size: 6553600 bytes
[OTA] Using update size: 1058311 bytes
[OTA] Update.begin() successful
[OTA] Chunk #50: received=2048, written=2048, total=102400/1058311 bytes
[OTA] Chunk #100: received=2048, written=2048, total=204800/1058311 bytes
...
[OTA] OTA End: 1058311 bytes total
[OTA] Total written: 1058311 bytes
[OTA] Chunks processed: 517
[OTA] Time elapsed: 5234 ms
[OTA] Average speed: 197.21 KB/s
[OTA] Free heap after OTA: 253188 bytes
[OTA] Calling Update.end(false) - skipping signature check...
[OTA] OTA Success: 1058311 bytes
[OTA] Firmware MD5: abc123...
```

## Тестирование

### 1. Компиляция проекта

```bash
pio run
```

Компиляция прошла успешно без ошибок.

### 2. Загрузка прошивки на устройство

```bash
pio run --target upload
```

### 3. Тестирование OTA обновления

1. Откройте веб-интерфейс: `http://<IP-адрес>:8080/`
2. Нажмите кнопку **🔄 OTA Update**
3. Выберите файл `.pio/build/esp32-s3-devkitc1-n16r8/firmware.bin`
4. Нажмите **Загрузить прошивку**
5. Наблюдайте за логами в Serial Monitor

### 4. Проверка логов

В Serial Monitor должны появиться подробные логи о процессе загрузки:
- Размер файла и свободной памяти
- Информация о разделах
- Прогресс загрузки (каждые 50 чанков)
- Скорость загрузки
- Результат обновления

## Возможные проблемы и решения

### Проблема: "Update.begin() failed"

**Причина**: Недостаточно свободной памяти или неправильный размер раздела.

**Решение**: 
- Проверьте свободную память в логах
- Убедитесь, что используете правильный файл `firmware.bin`
- Попробуйте уменьшить размер прошивки

### Проблема: "Update.write() failed"

**Причина**: Ошибка записи во флеш-память.

**Решение**:
- Проверьте целостность файла прошивки
- Убедитесь, что устройство имеет стабильное питание
- Попробуйте перезагрузить устройство

### Проблема: "Update.end() failed"

**Причина**: Преждевременное завершение загрузки или ошибка проверки.

**Решение**:
- Проверьте логи для определения причины
- Убедитесь, что файл прошивки полный и не поврежден
- Попробуйте загрузить файл снова

## Дополнительные улучшения

Для дальнейшего улучшения OTA можно рассмотреть:

1. **Добавление проверки MD5** - для проверки целостности файла
2. **Добавление прогресс-бара** - для отображения прогресса в веб-интерфейсе
3. **Добавление аутентификации** - для защиты от несанкционированных обновлений
4. **Использование HTTPS** - для безопасной передачи файла
5. **Добавление отката** - для возможности возврата к предыдущей версии

## Измененные файлы

- [`src/apiota.cpp`](src/apiota.cpp) - добавлено подробное логирование и исправлена инициализация Update
- [`src/apiota.h`](src/apiota.h) - без изменений
- [`src/api.cpp`](src/api.cpp) - без изменений (попытка увеличения таймаута не удалась из-за отсутствия соответствующего метода)

## Компиляция

Проект успешно скомпилирован:
- Flash: 16.5% (1080527 bytes from 6553600 bytes)
- RAM: 14.4% (47336 bytes from 327680 bytes)
- IRAM: 100.0% (16384 bytes from 16384 bytes) - ⚠️ IRAM заполнен полностью
