# WiFi Управление Роботом - Документация

## 📡 Обзор

Реализована система управления роботом через WiFi с веб-интерфейсом и REST API.

### Возможности

- ✅ WiFi точка доступа для прямого подключения
- ✅ REST API для управления всеми компонентами
- ✅ Адаптивный веб-интерфейс (HTML/CSS/JS)
- ✅ Управление сервоприводами с калибровкой
- ✅ Управление DC-моторами с регулировкой скорости
- ✅ CORS поддержка для внешних приложений

---

## 📁 Структура модулей

```
src/
├── rwifi.h/cpp         # WiFi подключение и настройка
├── api.h/cpp           # REST API сервер
├── ui.h/cpp            # HTML интерфейс управления
├── servo.h/cpp         # Управление сервоприводами
├── dcmotor.h/cpp       # Управление DC-моторами
└── main.cpp            # Главный модуль
```

> ⚠️ **Примечание:** Модуль назван `rwifi.h/cpp` (robot WiFi) для избежания конфликта с системным файлом `WiFi.h`.

---

## 🌐 WiFi Подключение

### Режим работы

ESP32-S3 создаёт **точку доступа** (Access Point):

| Параметр | Значение |
|----------|----------|
| SSID | `RobotAP` |
| Пароль | `12345678` |
| IP адрес | `192.168.4.1` |
| Порт | `8080` |

### Подключение к роботу

1. Включите робота
2. Найдите WiFi сеть `RobotAP`
3. Подключитесь с паролем `12345678`
4. Откройте браузер и перейдите на `http://192.168.4.1:8080`

---

## 🔌 REST API

### Базовый URL

```
http://192.168.4.1:8080/api
```

### Эндпоинты

#### 1. GET /api/status

Получение статуса системы.

**Запрос:**
```
GET /api/status
```

**Ответ:**
```json
{
  "status": "ok",
  "ip": "192.168.4.1",
  "servos_count": 4
}
```

---

#### 2. GET /api/servo

Получение текущего состояния всех сервоприводов.

**Запрос:**
```
GET /api/servo
```

**Ответ:**
```json
{
  "servos": [
    {
      "id": 0,
      "angle": 90,
      "min": 140,
      "max": 480
    },
    {
      "id": 1,
      "angle": 90,
      "min": 140,
      "max": 480
    },
    {
      "id": 2,
      "angle": 90,
      "min": 140,
      "max": 480
    },
    {
      "id": 3,
      "angle": 90,
      "min": 140,
      "max": 480
    }
  ]
}
```

---

#### 3. POST /api/servo

Установка угла сервопривода.

**Запрос:**
```
POST /api/servo
Content-Type: application/json

{
  "id": 0,
  "angle": 90
}
```

**Ответ:**
```json
{
  "success": true,
  "servo": 0,
  "angle": 90
}
```

**Параметры:**
- `id` (0-15) — номер канала сервопривода
- `angle` (0-180) — угол в градусах

---

#### 4. POST /api/servo/calibrate

Калибровка границ сервопривода.

**Запрос:**
```
POST /api/servo/calibrate
Content-Type: application/json

{
  "id": 0,
  "min": 140,
  "max": 480
}
```

**Ответ:**
```json
{
  "success": true,
  "servo": 0,
  "min": 140,
  "max": 480
}
```

**Параметры:**
- `id` (0-15) — номер канала
- `min` (0-4095) — минимальное значение импульса
- `max` (0-4095) — максимальное значение импульса

---

#### 5. GET /api/motor

Получение текущей скорости моторов.

**Запрос:**
```
GET /api/motor
```

**Ответ:**
```json
{
  "motorA": 0,
  "motorB": 0
}
```

---

#### 6. POST /api/motor

Установка скорости моторов.

**Запрос:**
```
POST /api/motor
Content-Type: application/json

{
  "motorA": 100,
  "motorB": -150
}
```

**Ответ:**
```json
{
  "success": true,
  "motorA": 100,
  "motorB": -150
}
```

**Параметры:**
- `motorA` (-255...255) — скорость мотора A
  - Положительное значение — вперёд
  - Отрицательное — назад
- `motorB` (-255...255) — скорость мотора B

---

#### 7. POST /api/motor/stop

Остановка всех моторов.

**Запрос:**
```
POST /api/motor/stop
```

**Ответ:**
```json
{
  "success": true,
  "message": "All motors stopped"
}
```

---

## 🎨 Веб-интерфейс

### Доступ

Откройте в браузере: `http://192.168.4.1:8080`

### Функционал

#### Панель сервоприводов

- **4 карточки сервоприводов** (Servo 0-3)
- **Ползунок** для установки угла (0-180°)
- **Кнопка "Применить"** — отправка команды на робот
- **Секция калибровки**:
  - Поля Min/Max для настройки границ импульсов
  - Кнопка "Калибровать"

#### Панель DC-моторов

- **2 карточки моторов** (Motor A, Motor B)
- **Кнопки управления**:
  - `◀` — движение назад (удерживать)
  - `■` — стоп
  - `▶` — движение вперёд (удерживать)
- **Отображение текущей скорости**
- **Кнопка "Остановить все моторы"**

#### Статус-бар

- Индикатор подключения
- IP адрес робота

### Особенности UI

- Адаптивный дизайн (мобильные/десктоп)
- Тёмная тема с неоновыми акцентами
- Анимированные уведомления
- Real-time обновление статусов

---

## 💻 Примеры использования

### JavaScript (Fetch API)

```javascript
// Установка угла сервопривода
async function setServoAngle(id, angle) {
  const response = await fetch('http://192.168.4.1:8080/api/servo', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ id, angle })
  });
  return await response.json();
}

// Запуск мотора вперёд
async function motorForward(speed) {
  const response = await fetch('http://192.168.4.1:8080/api/motor', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ motorA: speed, motorB: speed })
  });
  return await response.json();
}

// Калибровка сервопривода
async function calibrateServo(id, min, max) {
  const response = await fetch('http://192.168.4.1:8080/api/servo/calibrate', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ id, min, max })
  });
  return await response.json();
}
```

### Python (requests)

```python
import requests

BASE_URL = 'http://192.168.4.1:8080/api'

# Установка угла сервопривода
def set_servo_angle(servo_id, angle):
    response = requests.post(f'{BASE_URL}/servo', json={
        'id': servo_id,
        'angle': angle
    })
    return response.json()

# Управление моторами
def set_motors(speed_a, speed_b):
    response = requests.post(f'{BASE_URL}/motor', json={
        'motorA': speed_a,
        'motorB': speed_b
    })
    return response.json()

# Остановка всех моторов
def stop_all():
    response = requests.post(f'{BASE_URL}/motor/stop')
    return response.json()

# Пример использования
set_servo_angle(0, 90)  # Серво 0 на 90 градусов
set_motors(100, 100)    # Оба мотора вперёд
stop_all()              # Остановка
```

### cURL (командная строка)

```bash
# Статус системы
curl http://192.168.4.1:8080/api/status

# Получить состояние сервоприводов
curl http://192.168.4.1:8080/api/servo

# Установить угол сервопривода
curl -X POST http://192.168.4.1:8080/api/servo \
  -H "Content-Type: application/json" \
  -d '{"id":0,"angle":90}'

# Калибровка сервопривода
curl -X POST http://192.168.4.1:8080/api/servo/calibrate \
  -H "Content-Type: application/json" \
  -d '{"id":0,"min":140,"max":480}'

# Управление моторами
curl -X POST http://192.168.4.1:8080/api/motor \
  -H "Content-Type: application/json" \
  -d '{"motorA":100,"motorB":100}'

# Остановка моторов
curl -X POST http://192.168.4.1:8080/api/motor/stop
```

---

## 🔧 Настройка

### Изменение WiFi параметров

Откройте `src/rwifi.cpp` и измените:

```cpp
#define WIFI_SSID "RobotAP"           // Новое имя сети
#define WIFI_PASSWORD "12345678"      // Новый пароль
```

### Изменение порта сервера

Откройте `src/api.cpp` и измените:

```cpp
WebServer server(8080);  // Новый порт
```

### Добавление новых сервоприводов

1. Откройте `src/ui.cpp`
2. Добавьте новую карточку сервопривода в HTML
3. Обновите JavaScript для работы с новым каналом

---

## 🛠️ Сборка и загрузка

```bash
# Установка зависимостей
pio lib install

# Сборка
pio run

# Загрузка
pio run --target upload

# Монитор порта
pio device monitor
```

---

## 📊 Схема подключения

```
ESP32-S3          PCA9685
─────────────────────────
GPIO 8 (SDA)  ─── SDA
GPIO 9 (SCL)  ─── SCL
3.3V/5V       ─── VCC
GND           ─── GND

ESP32-S3          Motor Driver
──────────────────────────────
GPIO 10 (A_IA)  ─── IN1
GPIO 7  (A_IB)  ─── IN2
GPIO 12 (B_IA)  ─── IN3
GPIO 11 (B_IB)  ─── IN4
```

---

## ⚠️ Важные замечания

1. **Диапазон скорости моторов:** -255 до 255
   - Положительное значение — вперёд
   - Отрицательное — назад
   - 0 — остановка

2. **Калибровка сервоприводов:**
   - Min/Max значения зависят от модели сервопривода
   - Типичные значения для MG90S: 140-480
   - Не устанавливайте слишком широкий диапазон — это может повредить сервопривод

3. **WiFi покрытие:**
   - Радиус действия точки доступа ~30-50 метров
   - Избегайте помех от других WiFi сетей

4. **Питание:**
   - Убедитесь, что PCA9685 и моторы получают достаточное питание
   - Рекомендуется отдельный источник 5V для сервоприводов

---

## 🐛 Отладка

### Включение отладки

1. Откройте Serial Monitor (115200 бод)
2. Наблюдайте за логами:
   - Инициализация WiFi
   - Запуск HTTP сервера
   - Обработанные команды API

### Типичные проблемы

| Проблема | Решение |
|----------|---------|
| Не подключается к WiFi | Проверьте SSID/пароль в wifi.cpp |
| Страница не загружается | Убедитесь, что порт 8080 открыт |
| Сервоприводы не двигаются | Проверьте подключение PCA9685 |
| Моторы не работают | Проверьте пины и питание драйвера |

---

## 📄 API Reference (кратко)

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/api/status` | Статус системы |
| GET | `/api/servo` | Получить сервоприводы |
| POST | `/api/servo` | Установить угол |
| POST | `/api/servo/calibrate` | Калибровать |
| GET | `/api/motor` | Получить моторы |
| POST | `/api/motor` | Установить скорость |
| POST | `/api/motor/stop` | Остановить все |

---

## 🚀 Расширение функциональности

### Добавление новых команд API

1. Создайте обработчик в `src/api.cpp`
2. Зарегистрируйте маршрут в `api_init()`
3. Вызовите функцию из соответствующего модуля

### Интеграция с внешними системами

REST API позволяет интегрировать робота с:
- Мобильными приложениями
- Системами компьютерного зрения
- Умным домом (Home Assistant и др.)
- Собственными скриптами автоматизации

---

**Версия:** 2.0  
**Дата:** Февраль 2026
