# OTA Update (Over-The-Air)

Загрузка прошивки через WiFi без подключения по USB.

## Как использовать

### 1. Через веб-интерфейс

1. Откройте главную страницу робота: `http://<IP-адрес>:8080/`
2. Нажмите кнопку **🔄 OTA Update** в верхней панели
3. Перетащите `.bin` файл или выберите его кнопкой "Выбрать файл"
4. Нажмите **Загрузить прошивку**
5. Дождитесь завершения и автоматической перезагрузки

### 2. Через API

```bash
curl -X POST -F "firmware=@build/esp32-s3-devkitc1-n16r8/firmware.bin" http://<IP-адрес>:8080/api/ota/upload
```

## Эндпоинты

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/api/ota` | Страница загрузки прошивки (UI) |
| POST | `/api/ota/upload` | Загрузка .bin файла |

## Получение .bin файла

### PlatformIO

```bash
# Собрать прошивку
pio run

# Путь к файлу:
# .pio/build/esp32-s3-devkitc1-n16r8/firmware.bin
```

### Arduino IDE

1. **Sketch → Export Compiled Binary**
2. Файл находится в папке проекта

## Требования

- ESP32-S3 должен быть в той же WiFi сети
- Достаточно свободного места в flash памяти
- Файл должен иметь расширение `.bin`

## Возможные ошибки

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Not enough space` | Файл слишком большой | Очистите память или уменьшите размер прошивки |
| `Update.begin() failed` | Ошибка инициализации | Перезагрузите устройство |
| `Update.end() failed` | Ошибка записи | Проверьте целостность .bin файла |

## Безопасность

⚠️ **Внимание:** В текущей версии нет проверки подлинности прошивки. Злоумышленник в той же WiFi сети может загрузить вредоносную прошивку.

Для продакшена рекомендуется добавить:
- Проверку цифровой подписи
- HTTPS вместо HTTP
- Аутентификацию пользователя
