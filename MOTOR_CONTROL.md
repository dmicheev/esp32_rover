# 🎛️ Управление скоростью моторов

## Обзор

Реализовано удобное управление скоростью DC моторов с помощью:
- Ползунков для точной настройки
- Полей ввода числовых значений
- Кнопки для установки скорости всем моторам сразу

## 🎮 Элементы управления

### 1. Карточка каждого мотора (A, B, C, D)

Каждая карточка мотора содержит:

- **Кнопки управления:**
  - `◀` — движение назад (удерживать для скорости -255)
  - `■` — стоп (скорость 0)
  - `▶` — движение вперёд (удерживать для скорости 255)

- **Отображение текущей скорости:**
  - Числовое значение от -255 до 255

- **Ползунок для точной настройки:**
  - Диапазон: -255 ... 0 ... 255
  - Визуальная индикация направления цветом

- **Поле ввода:**
  - Прямой ввод числового значения
  - Кнопка "Применить" для отправки

### 2. Панель управления всеми моторами

Отдельная панель для одновременной установки скорости всех моторов:

- **4 поля ввода** (Motor A, B, C, D)
- **Кнопка "🚀 Установить скорость всех моторов"**
  - Отправляет одну команду со скоростями всех моторов
  - Обновляет все ползунки и отображения

## 📡 API запросы

### Установка скорости одного мотора

```json
POST /api/motor
{
  "motorA": 150
}
```

### Установка скорости всех моторов

```json
POST /api/motor
{
  "motorA": 100,
  "motorB": 100,
  "motorC": -50,
  "motorD": -50
}
```

**Ответ:**
```json
{
  "success": true,
  "motorA": 100,
  "motorB": 100,
  "motorC": -50,
  "motorD": -50
}
```

### Получение скорости всех моторов

```json
GET /api/motor
```

**Ответ:**
```json
{
  "motorA": 100,
  "motorB": 100,
  "motorC": -50,
  "motorD": -50
}
```

## 🎨 Визуальные элементы

### Ползунок скорости

- **Красный цвет** (-255...0) — движение назад
- **Серый цвет** (0) — остановка
- **Зелёный цвет** (0...255) — движение вперёд

### Уведомления

- **Зелёное** — успешная установка скорости
- **Красное** — ошибка (неверный диапазон, проблема соединения)

## 💻 JavaScript функции

### `updateMotorValue(motor, value)`
Обновляет отображение значения для мотора:
- Текстовое значение
- Положение ползунка
- Значение в поле ввода

### `setMotorFromSlider(motor)`
Отправляет скорость мотора из ползунка/поля ввода

### `setAllMotors()`
Устанавливает скорость всем моторам сразу одним запросом

## 📊 Примеры использования

### Пример 1: Установка скорости одного мотора

1. Переместите ползунок Motor A на значение 150
2. Нажмите "Применить"
3. Мотор A установится на скорость 150

### Пример 2: Установка скорости всем моторам

1. В панели "Управление всеми моторами" введите:
   - Motor A: 100
   - Motor B: 100
   - Motor C: -100
   - Motor D: -100
2. Нажмите "🚀 Установить скорость всех моторов"
3. Все моторы установятся одновременно

### Пример 3: Движение вперёд/назад

- **Вперёд:** удерживайте кнопку `▶`
- **Назад:** удерживайте кнопку `◀`
- **Стоп:** отпустите кнопку или нажмите `■`

## ⚙️ Диапазон скоростей

| Значение | Описание |
|----------|----------|
| -255 | Максимальная скорость назад |
| -128 | Половинная скорость назад |
| 0 | Остановка |
| 128 | Половинная скорость вперёд |
| 255 | Максимальная скорость вперёд |

## 🔧 Настройка

### Изменение диапазона

В файле `data/index.html` измените атрибуты `min` и `max`:

```html
<input type="range" min="-255" max="255" value="0">
<input type="number" min="-255" max="255" value="0">
```

### Изменение скорости по умолчанию

В файле `data/index.html` измените `value="0"` на нужное значение.

---

**Дата обновления:** Февраль 2026  
**Версия:** 2.0
