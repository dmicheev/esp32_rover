# 📁 Загрузка файлов в LittleFS

## Обзор

HTML страница интерфейса управления хранится в файловой системе LittleFS ESP32-S3, а не в коде программы. Это уменьшает размер прошивки и упрощает редактирование интерфейса.

## 📁 Структура файлов

```
esp32_S3/
├── data/
│   └── index.html      ← HTML страница интерфейса
├── src/
│   ├── ui.h/cpp        ← Модуль работы с LittleFS
│   └── ...
└── platformio.ini
```

## 🚀 Загрузка файлов в ESP32

### Способ 1: PlatformIO CLI

```bash
# Загрузка файлов из папки data в LittleFS
pio run --target uploadfs
```

### Способ 2: PlatformIO VS Code

1. Откройте командную палитру (`Ctrl+Shift+P` или `Cmd+Shift+P`)
2. Выберите **"PlatformIO: Upload Filesystem Image"**
3. Дождитесь завершения загрузки

### Способ 3: Полная прошивка (код + LittleFS)

```bash
# Сначала загрузите прошивку
pio run --target upload

# Затем загрузите файлы в LittleFS
pio run --target uploadfs
```

## ✅ Проверка загрузки

После загрузки откройте Serial Monitor:

```bash
pio device monitor
```

Вы должны увидеть:

```
=== LittleFS Initialization ===
LittleFS initialized successfully
index.html found
===============================
```

## 📝 Редактирование интерфейса

1. Откройте файл `data/index.html`
2. Внесите изменения (HTML, CSS, JavaScript)
3. Загрузите изменения:
   ```bash
   pio run --target uploadfs
   ```

## 🔧 Форматирование LittleFS

Если возникли проблемы с файловой системой:

```bash
# Очистка LittleFS
pio run --target clean
pio run --target uploadfs
```

## ⚠️ Важные замечания

1. **Папка data должна существовать** перед загрузкой
2. **Все файлы в папке data** будут загружены в корень LittleFS
3. **Максимальный размер** LittleFS зависит от размера flash-памяти
4. **Файл index.html** должен существовать для работы веб-интерфейса

## 📊 Использование памяти

После сборки проверьте использование памяти:

```
Building .pio/build/esp32-s3-devkitc1-n16r8/firmware.bin
========================= [SUCCESS] =========================
```

HTML страница занимает место в LittleFS, а не в коде программы, что освобождает ~15KB программной памяти.

---

## 🛠️ Расширенное использование

### Добавление дополнительных файлов

Вы можете добавить другие файлы в папку `data/`:

```
data/
├── index.html      ← Главная страница
├── style.css       ← Отдельный CSS файл
├── script.js       ← Отдельный JavaScript файл
└── config.json     ← Файл конфигурации
```

### Доступ к файлам из кода

```cpp
#include <LittleFS.h>
#include "ui.h"

// Чтение файла
File file = LittleFS.open("/config.json", "r");
if (file) {
  String content = file.readString();
  file.close();
}

// Запись в файл
File file = LittleFS.open("/log.txt", "w");
if (file) {
  file.println("Log entry");
  file.close();
}

// Проверка наличия файла
if (LittleFS.exists("/index.html")) {
  Serial.println("File exists!");
}

// Удаление файла
LittleFS.remove("/old_file.txt");
```

---

**Дата обновления:** Февраль 2026  
**Версия:** 1.0
