# Отчёт о реализации задачи "WiFi управление роботом"

## ✅ Статус реализации

**Все функции реализованы и готовы к использованию.**

---

## 🔔 Важное замечание

**Файл WiFi модуля назван `rwifi.h/cpp`** (вместо `wifi.h`) для избежания конфликта с системным заголовочным файлом `WiFi.h` библиотеки Arduino ESP32.

При изменении кода обращайтесь к файлам:
- `src/rwifi.h` — заголовочный файл
- `src/rwifi.cpp` — реализация

---

## 📁 Созданные файлы

### Новые модули

| Файл | Описание |
|------|----------|
| `src/rwifi.h` | Заголовочный файл WiFi модуля |
| `src/rwifi.cpp` | Подключение WiFi, создание точки доступа |
| `src/api.h` | Заголовочный файл REST API |
| `src/api.cpp` | HTTP сервер, обработчики запросов |
| `src/ui.h` | Заголовочный файл UI |
| `src/ui.cpp` | HTML страница веб-интерфейса |
| `WIFI_CONTROL.md` | Подробная документация по WiFi управлению |

> ⚠️ **Важно:** Файл назван `rwifi.h` (вместо `wifi.h`) для избежания конфликта с системным заголовочным файлом `WiFi.h` библиотеки Arduino.

### Обновлённые файлы

| Файл | Изменения |
|------|-----------|
| `src/servo.h` | Добавлены функции для API |
| `src/servo.cpp` | Реализованы функции установки/получения углов |
| `src/dcmotor.h` | Полная переработка с управлением скоростью |
| `src/dcmotor.cpp` | Реализовано управление 2 моторами |
| `src/main.cpp` | Интеграция всех модулей |
| `platformio.ini` | Добавлена библиотека ArduinoJson |
| `README.md` | Добавлен раздел WiFi управления |

---

## 🏗️ Архитектура решения

```
┌─────────────────────────────────────────────────────┐
│                    main.cpp                         │
│  (инициализация и основной цикл)                    │
└──────────────┬──────────────────────────────────────┘
               │
       ┌───────┼───────────────┐
       │       │               │
   ┌───▼───┐ ┌─▼──────┐  ┌────▼────┐
   │rwifi  │ │  api   │  │  servo  │
   │ .h/cpp│ │ .h/cpp │  │  .h/cpp │
   └───────┘ └───┬────┘  └────┬────┘
                 │            │
           ┌─────▼─────┐ ┌────▼────┐
           │   ui.h/   │ │dcmotor.h│
           │   ui.cpp  │ │  /cpp   │
           └───────────┘ └─────────┘
```

---

## 📡 WiFi Конфигурация

### Точка доступа

| Параметр | Значение |
|----------|----------|
| SSID | `netis_2.4G_E86_E86C87` |
| Пароль | `X498g8wsd0` |
| IP адрес | `192.168.4.1` |
| Порт | `8080` |

> Примечание: В файле `src/wifi.cpp` установлены реальные SSID и пароль.

---

## 🔌 REST API Reference

### Сервоприводы

#### GET /api/servo
Получить состояние всех сервоприводов.

**Ответ:**
```json
{
  "servos": [
    {"id": 0, "angle": 90, "min": 140, "max": 480},
    {"id": 1, "angle": 90, "min": 140, "max": 480},
    {"id": 2, "angle": 90, "min": 140, "max": 480},
    {"id": 3, "angle": 90, "min": 140, "max": 480}
  ]
}
```

#### POST /api/servo
Установить угол сервопривода.

**Запрос:**
```json
{"id": 0, "angle": 90}
```

#### POST /api/servo/calibrate
Калибровка границ сервопривода.

**Запрос:**
```json
{"id": 0, "min": 140, "max": 480}
```

### Моторы

#### GET /api/motor
Получить скорость моторов.

**Ответ:**
```json
{"motorA": 0, "motorB": 0}
```

#### POST /api/motor
Установить скорость моторов.

**Запрос:**
```json
{"motorA": 100, "motorB": -150}
```

**Параметры:**
- `motorA`: -255...255 (положительное — вперёд, отрицательное — назад)
- `motorB`: -255...255

#### POST /api/motor/stop
Остановить все моторы.

---

## 🎨 Веб-интерфейс

### Доступ
```
http://192.168.4.1:8080
```

### Компоненты

1. **Панель сервоприводов**
   - 4 карточки (Servo 0-3)
   - Ползунки для установки угла (0-180°)
   - Кнопки "Применить"
   - Секции калибровки (Min/Max)

2. **Панель DC-моторов**
   - 2 карточки (Motor A, Motor B)
   - Кнопки: ◀ (назад), ■ (стоп), ▶ (вперёд)
   - Отображение текущей скорости
   - Кнопка "Остановить все моторы"

3. **Статус-бар**
   - Индикатор подключения
   - IP адрес робота

---

## 💻 Примеры использования

### cURL

```bash
# Статус
curl http://192.168.4.1:8080/api/status

# Получить сервоприводы
curl http://192.168.4.1:8080/api/servo

# Установить угол
curl -X POST http://192.168.4.1:8080/api/servo \
  -H "Content-Type: application/json" \
  -d '{"id":0,"angle":90}'

# Калибровка
curl -X POST http://192.168.4.1:8080/api/servo/calibrate \
  -H "Content-Type: application/json" \
  -d '{"id":0,"min":140,"max":480}'

# Моторы
curl -X POST http://192.168.4.1:8080/api/motor \
  -H "Content-Type: application/json" \
  -d '{"motorA":100,"motorB":100}'

# Остановка
curl -X POST http://192.168.4.1:8080/api/motor/stop
```

### Python

```python
import requests

BASE = 'http://192.168.4.1:8080/api'

# Сервопривод
requests.post(f'{BASE}/servo', json={'id': 0, 'angle': 90})

# Моторы
requests.post(f'{BASE}/motor', json={'motorA': 100, 'motorB': 100})

# Остановка
requests.post(f'{BASE}/motor/stop')
```

### JavaScript

```javascript
// Установка угла сервопривода
fetch('/api/servo', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({id: 0, angle: 90})
});

// Управление моторами
fetch('/api/motor', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({motorA: 100, motorB: 100})
});
```

---

## 🔧 Сборка и развёртывание

### Требования

- PlatformIO (VS Code или CLI)
- Python 3.10-3.13
- ESP32-S3-DevKitC1-N16R8

### Команды

```bash
# Установка зависимостей
pio lib install

# Сборка
pio run

# Загрузка
pio run --target upload

# Монитор порта
pio device monitor
```

### Известные проблемы

**Ошибка Python 3.14:**
```
ERROR: Python version must be between 3.10 and 3.13.
Current Python version: 3.14.0
```

**Решение:** Использовать Python 3.10-3.13 для сборки.

---

## 📊 Схема подключения

```
ESP32-S3          PCA9685         Motor Driver
────────────────────────────────────────────────
GPIO 8 (SDA)  ─── SDA
GPIO 9 (SCL)  ─── SCL
GPIO 10 (A_IA) ─────────────────── IN1
GPIO 7  (A_IB) ─────────────────── IN2
GPIO 12 (B_IA) ─────────────────── IN3
GPIO 11 (B_IB) ─────────────────── IN4
```

  Сервоприводы:
     - Серво 0 → Мотор B (левая сторона)
     - Серво 1 → Мотор A (правая сторона)
     - Серво 2 → Мотор D (правая сторона)
     - Серво 3 → Мотор C (левая сторона)

    Стороны:
     - Левые: серво 0 (B) и серво 3 (C)
     - Правые: серво 1 (A) и серво 2 (D)

---

## 📝 Структура проекта

```
esp32_S3/
├── src/
│   ├── main.cpp           # Главный модуль
│   ├── pins.h             # Конфигурация пинов
│   ├── rwifi.h/cpp        # WiFi модуль
│   ├── api.h/cpp          # REST API
│   ├── ui.h/cpp           # Веб-интерфейс
│   ├── servo.h/cpp        # Сервоприводы
│   └── dcmotor.h/cpp      # DC-моторы
├── platformio.ini         # Конфигурация
├── README.md              # Основная документация
├── WIFI_CONTROL.md        # WiFi документация
└── IMPLEMENTATION.md      # Этот файл
```

---

## ✅ Проверка функциональности

### Чек-лист задачи

- [x] Подключить WiFi и вынести логику в отдельный файл
- [x] Сделать REST API для управления сервоприводами
- [x] Добавить калибровку сервоприводов через API
- [x] Сделать управление DC-моторами (скорость вращения)
- [x] Создать UI управления на HTML (порт 8080)
- [x] Реализовать независимое управление сервоприводами и моторами
- [x] Вынести всю логику в отдельные файлы

### Функциональность

| Компонент | Статус | Файлы |
|-----------|--------|-------|
| WiFi AP | ✅ | wifi.h/cpp |
| REST API (сервоприводы) | ✅ | api.h/cpp, servo.h/cpp |
| REST API (моторы) | ✅ | api.h/cpp, dcmotor.h/cpp |
| Веб-интерфейс | ✅ | ui.h/cpp |
| Serial управление | ✅ | servo.cpp (сохранено) |

---

## 🚀 Рекомендации по использованию

1. **Первая загрузка:**
   - Подключите ESP32-S3 по USB
   - Загрузите прошивку через `pio run --target upload`
   - Откройте Serial Monitor (115200 бод)

2. **Подключение к роботу:**
   - Найдите WiFi сеть `netis_2.4G_E86_E86C87`
   - Подключитесь с паролем `X498g8wsd0`
   - Откройте `http://192.168.4.1:8080`

3. **Настройка сервоприводов:**
   - Используйте ползунки для установки угла
   - При необходимости откалибруйте Min/Max
   - Нажмите "Применить"

4. **Управление моторами:**
   - Удерживайте ◀ или ▶ для движения
   - Нажмите ■ для остановки
   - Используйте "Остановить все" для аварийной остановки

---

**Дата реализации:** Февраль 2026  
**Версия:** 2.0  
**Статус:** Готово к использованию (требуется Python 3.10-3.13 для сборки)
